## Baixar os pacotes do sistema
     npm install
## Rodar o sistema de forma local
    npm run dev  |  yarn dev

## Gerar uma nova migration 
    yarn prisma migrate dev | npm run prisma migrate dev

